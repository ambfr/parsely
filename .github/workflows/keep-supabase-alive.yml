name: Keep Supabase Alive

on:
  schedule:
    - cron: '0 0 */5 * *'  # runs every 5 days
  workflow_dispatch:  # lets you trigger manually too

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase
        run: |
          - name: Ping Supabase
        run: |
          curl -s "https://ldpzebatkfivrifosapq.supabase.co/auth/v1/health"